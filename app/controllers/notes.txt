col_height = puzzle.height / puzzle.rows
row_width = puzzle.width / puzzle.rows
current_piece = 1

while current_row < puzzle.rows do
	current_col = 0
	while current_col < puzzle.rows do
		json.x_pos = current_col * row_width
		json.y_pos = current_col * col_height
		#stick in something to store it
		json.current_piece +=1
		current_col +=1
	end
	current_row +=1
end